Mô phỏng quá thực thi:

- Lúc client gửi request để tạo object thì Protection proxy sẽ yêu cầu người dùng nhập mật khẩu (mật khẩu này được lưu trữ ở .env và load thông qua thư viện config), nếu nhập đúng mật khẩu thì nó sẽ cho người dùng tạo đối tượng.
